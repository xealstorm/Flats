package com.kalachev.aviv.layer.data.local.model

/**
 * A helper model that represents a pair of localId and the corresponding id.
 */
data class LocalFlatIdPair(
    val localId: Long,
    val id: Long
)